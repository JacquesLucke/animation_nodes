cpdef TARGETLIST toTARGETLIST(numeric_list sourceList):
    cdef TARGETLIST newList = TARGETLIST(sourceList.length)
    cdef long i
    for i in range(len(sourceList)):
            newList.data[i] = <TYPE>sourceList.data[i]
    return newList
